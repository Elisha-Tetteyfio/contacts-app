<div class="page">
  <p style="color: green"><%= notice %></p>

  <div class="contactTop flex-row-reverse">
    <div class="contactNew btn align-right mt-2">
      <%= link_to "+ Add region", new_region_path %>
    </div>
  </div>

  <div>
    <% if @regions.empty? %>
      <h3 class="font-italic font-monospace font-weight-lighter mt-4">There are no regions to display!</h3>
    <% else %>
    <fieldset id="contacts" data-controller="region">
      <legend class="mb-4" style="color: rgb(72, 72, 116);">All regions</legend>
      <hr>
      <table class="table table-striped">
        <thead>
          <tr>
            <th class="header">#</th>
            <th class="header">Name</th>
            <th class="header">Remark</th>
            <th class="header">Added by</th>
          </tr>
        </thead>
        <tbody>
          <% i = 1 %>
          <% @regions.each do |region| %>
            <tr>
              <td> <%= i %> </td>
              <td> <%= region.name %> </td>
              <td> <%= region.remark %> </td>
              <td> <%= region.user.fname %> <%= region.user.lname %> </td>
              <td> 
                <div class="actions">
                    <%= link_to raw("<i class=\"bi-eye btn border\"></i>"), region%>
                    <%= link_to raw("<i class=\"bi-pen btn border\"></i>"), edit_region_path(region)%>
                    <button type="button" class="btn border" data-bs-target="#confirmationModal" data-bs-toggle="modal" action= "click->region#modall" >
                      <i class="bi-trash"></i>
                    </button>
                  
                    <div class="modal fade" id="confirmationModal" tabindex="-1" 
                      aria-labelledby="confirmationModalLabel" aria-hidden="true">
                        <div class="modal-dialog"> 
                          <div class="modal-content">                      
                            <div class="modal-header"> 
                            <h3>Confirmation</h3>                       
                            </div>                      
                            <div class="modal-body"> 
                            <div> Do you want to delete this region? </div>                       
                            </div>                      
                            <div class="modal-footer">  
                                                  
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>                        
                              <%= button_to raw("<i class=\"bi bi-trash\"></i> Delete"), region_path(region), class: "btn btn-danger", method: :delete, data: { confirm: "Are you sure you want to delete this region?" } %>                      
                      </div>
                    </div>
                  </div>
                </div> 
                  </div>  
                </div>
              </td>
            </tr>
            <% i += 1 %>
          <% end %>
        </tbody>
      </table>
    </fieldset>
  <% end %>
</div>

